# 本服务端口
PORT=3000

# 统一配置数据文件路径（可选，默认 server/data/config.json；含 push_*、lvyatech_* 等）
# DATA_CONFIG_PATH=

# 用户数据文件路径（可选，默认 server/data/users.json）
# USER_DATA_FILE=

# 推送消息按日存储目录（可选，默认 server/data/push-messages）
# PUSH_MESSAGE_DATA_DIR=

# 推送消息默认保留天数（可选，默认 7；可在后台配置覆盖）
# PUSH_MESSAGE_RETAIN_DAYS=7

# 是否启用推送消息定时清理（可选，默认不启用；设为 true/1/yes 开启）
# PUSH_MESSAGE_CLEANUP_ENABLED=false

# 推送消息清理任务执行间隔，毫秒（可选，默认 86400000 即 24 小时；仅当 CLEANUP_ENABLED 开启时生效）
# PUSH_MESSAGE_CLEANUP_INTERVAL_MS=86400000

# 控制指令：设备地址与 token（可选，也可在 data/config.json lvyatech_deviceUrl、lvyatech_token 中配置）
# LVYATECH_DEVICE_URL=http://192.168.7.170:38585
# LVYATECH_DEVICE_TOKEN=
