{
  "name": "message-forwarder",
  "private": true,
  "pnpm": {
    "overrides": {
      "is-generator-function": "1.0.10"
    }
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:server": "pnpm --filter @message-forwarder/server run dev",
    "dev:web": "pnpm --filter @message-forwarder/web run dev",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
